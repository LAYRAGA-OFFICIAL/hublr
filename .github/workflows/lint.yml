name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Check HTML
        run: echo "Lint placeholder — bisa ditambah htmlhint nanti"
      - name: Check JS
        run: echo "Lint placeholder — bisa ditambah eslint nanti"